/**
 * Stub for CSS imports under Jest (wired up in jest.config.js
 * `moduleNameMapper`). Jest cannot parse CSS, and the tests assert on
 * behaviour and semantics rather than computed styles, so an empty module is
 * all the import needs to resolve to.
 */
export default {};
